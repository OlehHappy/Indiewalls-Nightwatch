## Indiewalls test suite

do `npm install`

to run the test:

```
$ nightwatch -c ./Nightwatch.js --env default
```

## Test should have

* profile picture can be replaced
* change email works
* update name works
* change password works
